18.04.2026
Problem - Valid Palindrom
Stack is a STL ;
stact<data_type> variable_name ; // st
st.push(data); // add data into stack
st.pop() // removes the top element of the stack
st.empty() // return true if stack is empty otherwise false
st.top() // return the current top value of the stack 

Learning : assigning the top value of empty stack may give undefined value or crash program !!
